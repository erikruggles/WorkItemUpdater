{
  "name": "task",
  "version": "1.0.0",
  "description": "package.json for task lib",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vso-node-api": "^6.5.0",
    "vsts-task-lib": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "@types/q": "1.5.0"
  }
}
